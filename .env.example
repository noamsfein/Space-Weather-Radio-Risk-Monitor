# Optional. Leave OPENAI_API_KEY blank for the required deterministic replay.
# Each partner must use their own private project key; never commit a real key.
OPENAI_API_KEY=
# Shared optional live-briefing model used by both partners.
OPENAI_MODEL=gpt-5-nano
